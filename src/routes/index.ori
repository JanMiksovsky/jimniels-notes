(site) => <templates/Page.ori.html>({
  site,
  title: "Jim Nielsen’s Notes",
  // randomId.ori requires that children be text
  children: Tree.deepText(Tree.map(
    site.items,
    (item) => <templates/Note.ori.html>(item)
  )),
  path: "/"
})
